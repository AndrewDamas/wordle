import styled from "@emotion/styled";

export const KeyboardWrapper = styled.div({
    height: "190px",
    display: "flex",
    flexDirection: "column",
    gap: "8px",
    alignItems: "center",
    marginTop: "14px",
})